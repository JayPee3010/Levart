<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:Levart"
             x:Class="Levart.MainPage"
             Padding="0, 20, 0, 0">

        <ListView x:Name="albumListView" ItemSelected="OnItemSelected" RowHeight="60" Header="Albums" HorizontalOptions="Center">
            <ListView.Header>
                <Label XAlign="Center" FontSize="Medium"  Text="Albums" />
            </ListView.Header>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <StackLayout Orientation ="Vertical" Padding="10">
                            <Label XAlign="Center" Text="{Binding Name}"/>
                            <Label XAlign="Center" Text="{Binding UnitCost, StringFormat='${0}'}"
                                   TextColor="Green"/>
                        </StackLayout>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>


</ContentPage>